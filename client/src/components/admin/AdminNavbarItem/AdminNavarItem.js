import React from 'react';

const AdminNavarItem = ({
    textContent, sectionId, headerLocation, footerLocation, disabled
}) => {
    return (
        <div>
            
        </div>
    );
};

export default AdminNavarItem;